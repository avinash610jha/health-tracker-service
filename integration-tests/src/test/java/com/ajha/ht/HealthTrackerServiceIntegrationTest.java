package com.ajha.ht;

// import org.junit.Test;
// import org.junit.runner.RunWith;
// import org.springframework.boot.test.autoconfigure.web.servlet.AutoConfigureMockMvc;
// import org.springframework.boot.test.context.SpringBootTest;
// import org.springframework.test.context.junit4.SpringRunner;
// import org.springframework.test.web.servlet.request.MockMvcRequestBuilders;
// import static org.hamcrest.CoreMatchers.containsString;
// import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.jsonPath;
// import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.status;

// import org.springframework.test.web.servlet.MockMvc;
// import org.springframework.beans.factory.annotation.Autowired;

// @RunWith(SpringRunner.class)
// @SpringBootTest(classes = Application.class)
// @AutoConfigureMockMvc
public class HealthTrackerServiceIntegrationTest {

    // @Autowired
	// private MockMvc mockMvc;

    // @Test
	// public void testReadSymptoms() throws Exception {

    //     mockMvc.perform(MockMvcRequestBuilders.get("/odata/v4/HealthTrackerService/Symptoms")).andExpect(status().isOk())
    //     .andExpect(jsonPath("$.value[0].name").value(containsString("Fever")))
    //     .andExpect(jsonPath("$.value[0].since").value(containsString("28-MAR-2020")));

	// }

    
    
}
